spring:
  application:
    name: cartera-temporal-microservice
  profiles:
    active: 
      - production
  cloud:
    config:
      enabled: true
      
      
 
---      
spring:
  profiles: develop
  cloud:
    config:
      uri: http://config-microservice:8888
---
spring:
  profiles: production
  cloud:
    config:
      failFast: true
      uri: http://config-microservice:8888
      

